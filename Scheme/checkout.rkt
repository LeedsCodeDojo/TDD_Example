#lang racket

(define apple-price 10)

(define (calculate-total items)
  apple-price)
  
(provide calculate-total)






;(define prices
;  (hash
;   "Apple" 10
;   "Banana" 25))

;(define (calculate-total items)
;  (lookup-price (first items) prices))

;(define (lookup-price item prices)
;  (hash-ref prices item))